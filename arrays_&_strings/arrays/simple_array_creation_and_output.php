<?php
// Create an array of colors
$colors = array("Red", "Blue", "Green", "Yellow", "Purple");

// Loop through the array using a for loop
for ($i = 0; $i < count($colors); $i++) {
    // Output each color on a new line
    echo $colors[$i] . "<br>";
}
?>